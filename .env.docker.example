# Copiar a `.env` en la raíz del repo (junto a docker-compose.yml).
# Usá contraseña fuerte; si tiene caracteres especiales, URL-encode al armar DATABASE_URL a mano.

POSTGRES_PASSWORD=cambiar_por_clave_larga

# Puerto publicado del front (Nginx). 80 por defecto.
# HTTP_PORT=80
