# Puerto del servidor HTTP
PORT=3001

# Opción A — una sola URL (recomendado en hosting)
# DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/nombre_base

# Opción B — variables por separado (local o sin URL única)
PGHOST=localhost
PGPORT=5432
PGUSER=tu_usuario
PGPASSWORD=tu_contraseña
PGDATABASE=nombre_de_tu_base

# Esquema de tablas (en pgAdmin: web.exposiciones → PGSCHEMA=web)
PGSCHEMA=web

# IDs de club (web.clubes.id_club) desde los que pueden darse usuarios administradores (categoría 1).
# Lista separada por comas si hay varios. Por defecto 1 (= seed demo). Opcional también ID_CLUB_FCA (uno solo).
# ID_CLUBES_FCA=1
