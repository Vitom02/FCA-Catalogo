# Kennel FCA

Proyecto con **aplicación web** (`web/`) y carpeta reservada para la **API** (`api/`).

- Desarrollo web: `cd web` → `npm install` → `npm run dev`
- Descripción detallada de carpetas: ver **`ESTRUCTURA.md`**
